##############################
# OBJETOS E  FUNÇÕES BÁSICAS #
##############################

a <- 12
b <- "pastel"

# Função combine, c(), cria vetores (variáveis)
c <- c(3, 18, 27) # Vetor de Número
d <- c("abacaxi", "laranja", "uva") # Vetor de Strings

# Função paste() une infomrações textuais
paste("Minha casa é", b, sep = " ")

# Funções de estatística básica

mean(c)
median(c)
min(c)
max(c)
summary(c)
